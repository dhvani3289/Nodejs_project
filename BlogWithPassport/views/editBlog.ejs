<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>addBlog</title>
</head>
<style>
    .add-btn {
        text-align: center;
    }

    button {
        cursor: pointer;
    }

    .add-btn button {
        padding: 5px;
        width: 100%;
    }
</style>

<body>
    <%- include('header.ejs') %>
        <form method="post" action="/admin/updateBlog/<%= editBlog._id %>" enctype="multipart/form-data">
            <table border="1" align="center" cellPadding="10">
                <thead>
                    <tr>
                        <td> Image </td>
                        <td>
                            <input type="file" name="blogImage" value="<%= editBlog.blogImage %>" />
                        </td>
                    </tr>
                    <tr>
                        <td>Title</td>
                        <td>
                            <input type=" text" name="title" placeholder="Title" value="<%= editBlog.title %>" />
                        </td>
                    </tr>
                    <tr>
                        <td> Description</td>
                        <td>
                            <textarea name="description" placeholder="Description" cols={21} rows={3}><%=editBlog.description %>
                            </textarea>
                        </td>
                    </tr>
                    <tr>
                        <td>Date</td>
                        <td>
                            <input type="date" name="blogDate" value="<%= editBlog.blogDate %>" />
                        </td>
                    </tr>
                    <tr>
                        <td>Category</td>
                        <td>
                            <select name="category" value="<%= editBlog.category %>">
                                <option value="entertainment">Entertainment</option>
                                <option value="technology">Technology</option>
                                <option value="sports">Sports</option>
                                <option value="business">Business</option>
                                <option value="health">Health</option>
                                <option value="science">Science</option>
                            </select>
                        </td>
                    </tr>
                    <tr>
                        <td colSpan="2" class="add-btn">
                            <button type="submit">Edit Blog</button>
                        </td>
                    </tr>
                </thead>
            </table>
        </form>
</body>

</html>